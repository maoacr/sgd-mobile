/**
 * Tipos de la base de datos de Supabase — generados automáticamente.
 *
 * Este archivo es un PLACEHOLDER. Una vez creado el proyecto en Supabase
 * y aplicadas las migraciones SQL, reemplazarlo con los tipos reales:
 *
 *   npx supabase gen types typescript \
 *     --project-id <project-ref> \
 *     --schema public \
 *     > types/database.ts
 *
 * Hasta entonces, 'Database' es un tipo vacío que satisface al cliente.
 */
export type Database = Record<string, never>;
